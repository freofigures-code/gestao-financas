"use client";import{useEffect,useState}from"react";import{createClient}from"@/lib/supabase/client";export type CustomColumn={id:string,table_name:string,key:string,label:string,data_type:'text'|'number'|'date'|'boolean',position:number};export function useCustomColumns(tableName:string){const[columns,setColumns]=useState<CustomColumn[]>([]);useEffect(()=>{createClient().from('custom_columns').select('*').eq('table_name',tableName).order('position').then(({data})=>setColumns((data??[]) as CustomColumn[]))},[tableName]);return columns}
