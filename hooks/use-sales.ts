"use client";import{useEffect,useState}from"react";import{createClient}from"@/lib/supabase/client";import{monthStart,nextMonthStart}from"@/lib/date";
export function useSales(month:string){const[data,setData]=useState<any[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const refresh=async()=>{setLoading(true);const s=createClient();const{data,error}=await s.from('sales').select('id,order_sn,sold_at,status,gross_total,shopee_fee_total,shopee_net_total,production_cost_total,net_profit_total,source,custom_fields,sale_items(id,variant_id,quantity,unit_gross,product_name_snapshot,variant_name_snapshot)').gte('sold_at',monthStart(month)).lt('sold_at',nextMonthStart(month)).order('sold_at',{ascending:false});setData(data??[]);setError(error?.message??null);setLoading(false)};useEffect(()=>{refresh()},[month]);return{data,loading,error,refresh}}
