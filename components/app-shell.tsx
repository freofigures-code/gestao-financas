"use client"; import {Sidebar} from "@/components/sidebar"; import {MonthProvider} from "@/components/month-provider"; import {MonthSelector} from "@/components/month-selector"; import {Button} from "@/components/ui/button"; import {createClient} from "@/lib/supabase/client"; import {useRouter} from "next/navigation"; import {Toaster} from "sonner";import{MobileNav}from"@/components/mobile-nav";import{ThemeToggle}from"@/components/theme-toggle";
export function AppShell({children}:{children:React.ReactNode}){const router=useRouter(); const logout=async()=>{await createClient().auth.signOut(); router.replace('/login'); router.refresh();}; return <MonthProvider><div className="min-h-screen flex"><Sidebar/><main className="flex-1 min-w-0"><header className="min-h-16 border-b bg-background/95 sticky top-0 z-30 flex flex-wrap items-center justify-between gap-2 px-3 md:px-6 py-2"><div className="font-semibold lg:hidden">Freo Figures</div><MonthSelector/><div className="flex"><ThemeToggle/><Button variant="ghost" size="sm" onClick={logout}>Sair</Button></div></header><MobileNav/><div className="p-4 md:p-6 max-w-[1600px] mx-auto">{children}</div></main></div><Toaster richColors position="top-right"/></MonthProvider>}
