"use client";import{useEffect,useState}from"react";import{createClient}from"@/lib/supabase/client";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label";import{Button}from"@/components/ui/button";import{toast}from"sonner";import{todaySaoPaulo,currentMonthKey,maxDateForMonth}from"@/lib/date";import{useCustomColumns}from"@/hooks/use-custom-columns";
export function CashEntryForm({type,month,onDone}:{type:'income'|'expense',month:string,onDone:()=>void}){const columns=useCustomColumns(type==='income'?'income':'expenses');const[custom,setCustom]=useState<Record<string,any>>({});const[cats,setCats]=useState<any[]>([]);const[form,setForm]=useState({date:month===currentMonthKey()?todaySaoPaulo():`${month}-01`,description:'',amount:'0.00',category_id:''});useEffect(()=>{setForm(f=>({...f,date:month===currentMonthKey()?todaySaoPaulo():`${month}-01`}))},[month]);useEffect(()=>{createClient().from('categories').select('id,name').eq('type',type).order('name').then(({data})=>setCats(data??[]))},[type]);async function submit(e:React.FormEvent){e.preventDefault();const s=createClient();const payload={category_id:form.category_id,description:form.description,amount:form.amount,custom_fields:custom,[type==='income'?'received_at':'spent_at']:form.date};const{error}=await s.from(type==='income'?'income':'expenses').insert(payload);if(error)return toast.error(error.message);toast.success(type==='income'?'Entrada adicionada':'Saída adicionada');onDone()}return <form onSubmit={submit} className="grid gap-3 md:grid-cols-4"><div><Label>Data</Label><Input type="date" min={`${month}-01`} max={maxDateForMonth(month)} value={form.date} onChange={e=>setForm({...form,date:e.target.value})}/></div><div><Label>Categoria</Label><select required className="h-10 w-full rounded-md border bg-background px-3" value={form.category_id} onChange={e=>setForm({...form,category_id:e.target.value})}><option value="">Selecione</option>{cats.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></div><div><Label>Descrição</Label><Input required value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></div><div><Label>Valor</Label><Input required inputMode="decimal" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value.replace(',','.')})}/></div>{columns.map(c=><div key={c.id}><Label>{c.label}</Label>{c.data_type==='boolean'?<select className="h-10 w-full rounded-md border bg-background px-3" value={String(custom[c.key]??false)} onChange={e=>setCustom({...custom,[c.key]:e.target.value==='true'})}><option value="false">Não</option><option value="true">Sim</option></select>:<Input type={c.data_type==='date'?'date':c.data_type==='number'?'number':'text'} value={custom[c.key]??''} onChange={e=>setCustom({...custom,[c.key]:e.target.value})}/>}</div>)}<div className="md:col-span-4"><Button size="sm">Adicionar {type==='income'?'entrada':'saída'}</Button></div></form>}
